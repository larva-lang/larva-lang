public void main()
{
    /*
    说明：
    _Iter是__builtins模块的私有类型，但由于不是指定模块来访问（实际也没法显式import __builtins），不应该认为开发者的意图是故意import它，
    大概率是本模块的_Iter而忘了定义，因此不应该报“无访问权限”而应该报“未定义标识符”
    */
    _Iter<int> s;
}
