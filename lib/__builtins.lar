//内置builtin模块

public final class String
{
    public String(char[] arr)
    {
        s = new char[arr.len];
        for (ulong i = 0; i < arr.len; ++ i)
        {
            s[i] = arr[i];
        }
    }

    char[] s;
}
