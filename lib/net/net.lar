public native class TcpListener
{
    TcpListener();

    public TcpConn accept();
    public String addr();
}

public native TcpListener listen_tcp(String addr);

public native class TcpConn
{
    TcpConn();

    public String addr();
    public String remote_addr();
    public long send(char[] buf, long begin, long len);
    public long recv(char[] buf, long begin);
}
